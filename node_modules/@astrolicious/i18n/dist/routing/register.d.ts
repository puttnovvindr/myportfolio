import { HookParameters, AstroIntegrationLogger } from 'astro';
import { Options } from '../options.js';
import { Route } from '../types.js';
import 'sitemap';
import 'astro/zod';

declare const registerRoutes: (params: HookParameters<"astro:config:setup">, options: Options, logger: AstroIntegrationLogger) => {
    routes: Route[];
};

export { registerRoutes };
